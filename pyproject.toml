[project]
name = "pressure-room"
version = "0.1.0"
description = "A causal story-development workspace for pressure, choice, consequence, and moral change."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.41",
    "pandas>=2.2",
    "plotly>=5.24",
]

[tool.uv]
package = false
